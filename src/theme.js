export let theme = {
  primary: "#7F3FBF",
  secondary: "#7F7FFF",
  // accent: [255, 255, 255],
  accent: [0, 0, 0],
  breakpoint: 600,
  green: "#1B1A17",
   
};

// theme.textShadow = `0px 0px 15px rgba(${theme.accent[0]}, ${theme.accent[1]}, ${theme.accent[2]}, 0.4)`;

theme.textShadow = `0px 3px 5px rgba(${theme.accent[0]}, ${theme.accent[1]}, ${theme.accent[2]}, 0.4)`;
